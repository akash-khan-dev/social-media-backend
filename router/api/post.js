const router = require("express").Router();

const verifiedUserController = require("../../controllers/verifiedUser");

// router.post("/createPost", registrationController);

module.exports = router;
